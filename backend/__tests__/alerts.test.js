const { evaluateAlerts, validateAlertRule } = require('../services/alerts/alertEngine');
const farm = { id: 'f1', name: 'Demo', area: { value: 2, unit: 'acre' }, location: { latitude: 28, longitude: 77 } };
test('validates and evaluates deterministic thresholds', () => { const rule = { id: 'rain', type: 'heavy_rain', threshold: 20 }; expect(validateAlertRule(rule)).toEqual([]); const alerts = evaluateAlerts({ farm, weather: { precipitationMm: 25, provider: 'open-meteo' }, rules: [rule], now: '2025-01-01T00:00:00Z' }); expect(alerts).toHaveLength(1); expect(alerts[0].trigger.observedValue).toBe(25); });
test('handles decline and stale observations', () => { const rules = [{ id: 'veg', type: 'vegetation_decline', threshold: 0.1 }, { id: 'stale', type: 'stale_observation', maxAgeDays: 7 }]; const fieldHealth = { provider: { id: 'fixture' }, trend: { direction: 'declining', delta: -0.2 }, observation: { observationDate: '2025-01-01' } }; expect(evaluateAlerts({ farm, fieldHealth, rules, now: '2025-01-10T00:00:00Z' })).toHaveLength(2); });
